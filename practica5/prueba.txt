(setq a 1)
(setq b 2)
(setq c 10)
(defun square ()
  (print (* a a))            
  (print (+ b b))            
)
(defun sum_and_diff ()
  (print (+ a b))            
  (print (- c a))            
)
(defun main ()
  (print (* (+ a b) c))   
  (print (- c b))           
)
