(setq primo 0)
(setq n 0)
(setq m 0)
(setq d 0)
(setq i 0)
(setq p 0)
(defun main ()
	(setf i 1)
	(setf n 1)
	(setf m 100)
	(setf i 2)
	(loop while (< i m) do
	(progn	(setf primo 1)
	(progn	(setf d 2)
	(progn	(loop while (< d i) do
	(progn	(if (= (mod i d) 0)
	(progn	(setf primo 0)))
	(progn	(setf d (+ d 1)))))
	(progn	(if (/= primo 0)
	(progn	(princ i)
	(princ " ")
	))
	(progn	(setf i (+ i 1))))))))
)
 (main) 
