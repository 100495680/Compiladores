#include <stdio.h>
int a = 1;
int b = 0;
int c = 2;
main() {

    // mezcla &&, ||, ==, !=, <, >, <=, >=, %, +, -, *, /
    if ((a + c * 3) % 2 == 1 && b != 0 || a <= c) {
        puts("OK");
    }
}
//@ (main)
---------------------------------------------------
OK
---------------------------------------------------
(setq a 1)
(setq b 0)
(setq c 2)
(defun main ()
	(if (or (and (= (mod (+ a (* c 3)) 2) 1) (/= b 0)) (<= a c))
	(progn	(print "OK")))
)
 (main)
---------------------------------------------------

"OK" 
---------------------------------------------------

variable a
1 a !
variable b
0 b !
variable c
2 c !
: main a @ c @ 3 * + 2 mod 1 = b @ 0 = 0= and a @ c @ <= or if 
	." OK" 
then ;
main
---------------------------------------------------
Unknown option: -q
OK